package temp;
/*
import lombok.Getter;
import lombok.RequiredArgsConstructor;

import static co.myfair.core.exhibition.subexpo.model.hall.QHall.hall;

@RequiredArgsConstructor
@Getter
public enum HallSearchType {
    NAME("name", hall.name::eq),
    LOCATION("location", keyword -> hall.location.eq(keyword)),
    NUM("num", keyword -> hall.num.eq(Long.parseLong(keyword))),
    COUNTRY("country", keyword -> hall.country.eq(keyword));

    private final String name;
    private final TonyInterface function;

}
*/